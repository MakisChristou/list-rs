[package]
name = "todo-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
cursive = { version = "0.16.3", features = ["pancurses"] }
rusqlite = { version = "0.29.0", features = ["bundled"] }
